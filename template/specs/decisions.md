# Decisions

<!--
Durable facts. Append-only: supersede an entry by adding a new one that says so, never
by editing history out. Not read every session — opened when work touches what an entry
covers, or on request.

Record a choice here when it constrains future work. Skip anything recoverable by
reading the code.

Format, newest last:

## <short title in the present tense>

Date: YYYY-MM-DD

<What was decided. One or two sentences, specific enough to act on.>

Why: <the constraint that forced it>

The Why line is the whole point. A decision without its reason gets relitigated the
first time it is inconvenient, and unlike the decision itself the reason cannot be
recovered from the diff. If you cannot name a constraint, this is a preference and it
belongs in the Conventions section of specs/project.md instead.

Delete this comment once the first real entry lands.
-->
