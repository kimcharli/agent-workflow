# Improvements

<!--
Proposed changes to AGENTS.md, produced by ordinary work. Not read every session.
See the Improvement Proposals section of AGENTS.md for the loop this file feeds.

Append an entry the moment a human corrects the agent, in the same turn. The reason is
the part that stops being recoverable once the turn ends.

Format:

## <what went wrong, stated as friction rather than as a rule>

Status: Watching | Proposed | Resolved
First seen: YYYY-MM-DD
Struck: <dates of each recurrence>

Friction: <what actually happened, concretely>
Proposal: <the smallest change to AGENTS.md that would prevent it>
Resolution: <mechanize and delete | rewrite | delete | add — and why not an earlier one>
Retires when: <what would make the rule removable, usually "when a check enforces this">

Status rules:
- First occurrence opens at Watching. A second strike promotes it to Proposed.
- Something clearly severe may open at Proposed, if the entry says why.
- Resolved entries stay, so the same argument is not had twice.
- At review, delete any Watching entry you no longer recognise as real friction.
  Nothing else removes them, and a file of stale entries is the failure this loop
  exists to prevent.

An entry that adds lines to AGENTS.md when it is at its cap must name what it deletes
or merges to pay for them.

Delete this comment once the first real entry lands.
-->
