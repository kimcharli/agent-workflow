name: agent-workflow

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check the agent workflow contract
        run: sh scripts/agent-workflow-check.sh
